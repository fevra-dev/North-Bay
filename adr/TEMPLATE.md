# ADR-NNNN — <Title>

**Status:** Proposed | Accepted | Superseded · **Date:** YYYY-MM-DD
**Supersedes:** — · **Superseded-by:** —

## Context
<What forces are at play? Why is a decision needed now? Prefer a concrete failure over an
abstraction — "the theme toggle did nothing" is more useful to a future reader than "theming was
inconsistent".>

## Decision
<The rule. Imperative and testable. e.g. "Resting colours are declared in `@layer components`;
interaction states are declared in `@layer utilities`.">

## Considered Options
<!-- The alternatives weighed and why each lost, so a future reader does not re-litigate a
     settled call. One line each; the chosen option first. -->
- **<Option A — chosen>** — why it won.
- **<Option B>** — rejected: <why.>

## Consequences
<Trade-offs. What gets easier, what gets harder, what a future contributor now has to remember.>

## Enforcement
<The test or lint rule that fails on violation, by name and file. An ADR without enforcement is a
comment, not a control — if nothing catches the violation, the decision gets reversed by accident.>
