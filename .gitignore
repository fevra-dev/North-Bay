# deps / build
node_modules/
dist/
build/
coverage/
.next/
.vite/

# env
.env
.env.*
!.env.example

# macOS
.DS_Store

# logs
*.log
npm-debug.log*

# Playwright screenshots are regenerated by `npm run verify`
tests/screenshots/

# Local-only: agent instructions and application prep material.
# `adr/` is deliberately NOT here — the decision records are part of the project.
.claude/
CLAUDE.md
Research/
Refernce Original Site/

# Local working references and screenshots dropped in for review — not repo content.
fix/
